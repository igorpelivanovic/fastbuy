import { QuanityTotalCostPipe } from './quanity-total-cost.pipe';

describe('QuanityTotalCostPipe', () => {
  it('create an instance', () => {
    const pipe = new QuanityTotalCostPipe();
    expect(pipe).toBeTruthy();
  });
});
