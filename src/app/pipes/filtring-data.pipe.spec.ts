import { FiltringDataPipe } from './filtring-data.pipe';

describe('FiltringDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltringDataPipe();
    expect(pipe).toBeTruthy();
  });
});
