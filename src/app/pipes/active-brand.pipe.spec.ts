import { ActiveBrandPipe } from './active-brand.pipe';

describe('ActiveBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
