import { RatingValuePipe } from './rating-value.pipe';

describe('RatingValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RatingValuePipe();
    expect(pipe).toBeTruthy();
  });
});
