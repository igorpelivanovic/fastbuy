import { WhishlistBtnPipe } from './whishlist-btn.pipe';

describe('WhishlistBtnPipe', () => {
  it('create an instance', () => {
    const pipe = new WhishlistBtnPipe();
    expect(pipe).toBeTruthy();
  });
});
